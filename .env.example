# The dashboard derives the API host from the browser's address bar
# (window.location.hostname), so no base URL needs to be configured.
# Only the API port is configurable; it defaults to 7075.
VITE_API_PORT=7075
